<view class="app">
    <view class="app-title">GIS工具助手，提高工作效率</view>

    <block wx:for="{{funcList}}" wx:for-item="func" wx:key="index">
        <view class="app-con card">
            <navigator class="nav" url="{{func.path}}">{{func.title}}</navigator>
        </view>
    </block>
</view>